---
import Hero from '../components/Hero.astro';
import Layout from '../layouts/Layout.astro';
import BaseContainer from '../containers/BaseContainer.astro';
import getAllUsers from '../api/users';
import Card from '../components/TestimonyCard.astro';
const data = await getAllUsers();
const users = data?.users;
---

<Layout title="PAGE #3 " description='pagina demo'>
	<Hero title='Educación de calidad, a tu alcance' description='Simple, flexible y transparente' subTitle='' button='otro boton'/>
	<BaseContainer title='Testimonios'>
	{users?.map(user => <Card user={user} />)}
	</BaseContainer>
</Layout>

<style>
	h1 {
		font-size: 4rem;
		font-weight: 700;
		line-height: 1;
		text-align: center;
		margin-bottom: 1em;
	}
</style>
